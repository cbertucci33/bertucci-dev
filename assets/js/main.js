// bertucci.dev — base JS (design layer hooks added later)
document.addEventListener('DOMContentLoaded', function () {
  // Reserved for icon-system and interactive hooks once design lands.
  console.log('bertucci.dev ready');
});
